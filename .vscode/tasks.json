{
    "tasks": [
        {
            "type": "shell",
            "label": "Build Current Project [Debug]",
            "command": "make",
            "args": [ "debug" ],
            "options": { "cwd": "${workspaceFolder}" },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Generate Debug Executable"
        },
        {
            "type": "shell",
            "label": "Run [Debug]",
            "command": "make",
            "args": [ "rundbg" ],
            "options": { "cwd": "${workspaceFolder}" },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Run Debug Executable"
        }
    ],
    "version": "2.0.0",

}